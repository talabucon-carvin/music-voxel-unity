using UnityEngine;

public class RailBlock : MonoBehaviour
{
    // Optional: metadata like tempo, measure markers, etc.
    public int bpmOverride = 0;
    public Vector3 forwardDir = Vector3.forward;

    // Later you can add:
    // public Material highlightMaterial;
    // public Material normalMaterial;
}
